

====================================================================================
02 - Core Java
====================================================================================
1. What does a high-order bit represent for an integer?
2. What is the result of the following code snippet?
3. What is the result of the following code snippet?
4. What is the default value assigned to a variable of boolean type?
5. Choose an invalid way to declare a character variable in Java.
6. What do you mean by type promotion in Java?
7. Which of the following is the valid way to represent a long literal in Java?
8. What kind of error will be thrown if the main method is not found in the Java program?
9. Is Java a Strictly Typed Language?
10. Which method is used to add a new line character at the end of the output in the console?
11. What will be the value of variables ‘x’ and ‘y’ from the given code?
12. Predict the output of the code:
13. What will be the output of the following Java code?


====================================================================================
03 - Advance Java
====================================================================================
1. Can an abstract class be instantiated directly with the new operator in Java?
2. In what order are the constructors executed when a class hierarchy is created?
3. What is the purpose of the lambda operator (->) in Java lambda expressions?
4. Can a subclass reference variable be assigned to a superclass reference variable?
5. What is the output of the code?
6. Comment on the statement: "The method defined by a lambda expression does not have a name."
7. What is the output of the code?
8. By default, the methods enclosed within a Java interface are characterized as:
9. When implementing a Java interface, methods must possess the following access modifier:
10. What Java keyword is employed for specifying a default method within a Java interface?
11. Determine the capacity of the StringBuffer before and after the use of trimToSize() method in the given code:
12. What will be the value of the s1 and s2 variables after the execution of the following code?
13. Which class can be used when multiple threads are involved and thread-safety is required?
14. Which constructor of StringBuffer reserves room for 16 characters without reallocation?
15. What will be the output of the given code snippet?
16. What interface is implemented by the String, StringBuffer, and StringBuilder classes?
17. What is the return type of overridden methods?
18. Can a class be a superclass of itself in Java?
19. Which keyword is used to achieve inheritance in Java?
20. Which keyword is used to define a class member that can be accessed without reference to any object?
21. Does the given code defines the valid way to initialize the value to the final variable?
22. Can the value of a variable be changed if it is initialized with the 'final' keyword?
23. Which principle emphasizes the binding of data and methods within a class?

====================================================================================
04 - Maven
====================================================================================
Here are the questions extracted from your HTML:

1. In the provided Maven dependency, what does `<groupId>` refer to?
2. What does the `<dependency>` element represent in a Maven project configuration?
3. What is the full form of POM?
4. In Maven, what is a transitive dependency?
5. Which of the following elements in the pom.xml file specifies the project's group identifier?
6. In Maven, What is the default scope?
7. Which phase of the Maven build lifecycle is responsible for compiling the Java source code?
8. Which file is used to configure project settings and dependencies in Maven?
9. What is Maven primarily used for in Java development?

====================================================================================
05 - JDBC Data base connect
====================================================================================
1. What is the main benefit of using `PreparedStatement` over `Statement`?
2. Why is using `Statement` in JDBC considered risky?
3. Which CRUD operation does `executeUpdate()` typically support in JDBC?
4. What does `ResultSet` in JDBC represent?
5. Which JDBC interface is used to execute SQL queries and retrieve results?
6. Which external file is needed to connect PostgreSQL with Java using JDBC?
7. What is the correct order of steps in JDBC programming?
8. Which of the following is required to set up PostgreSQL for JDBC?
9. What is the primary purpose of JDBC in Java?

====================================================================================
06 - Hibernate
====================================================================================
1. Which caching mechanism does Hibernate Level 2 cache use by default?
2. What is the difference between `get()` and `load()` in Hibernate?
3. How can you apply filters and fetch only selected fields in HQL?
4. Which method fetches data using HQL?
5. What is HQL in Hibernate?
6. Which annotation is used for caching in Hibernate?
7. In Hibernate, what is eager fetching?
8. Which Hibernate annotation is used to map many-to-many relationships?
9. Which annotation is used for `OneToMany` and `ManyToOne` relationships?
10. What does `@OneToOne` mapping define in Hibernate?
11. Which Hibernate annotation is used for reusable embeddable objects like address?
12. How do you change the table and column names in Hibernate mappings?
13. Which Hibernate methods are used to update or delete objects?
14. Which method is used to fetch data in Hibernate?
15. Why is refactoring important in a Hibernate project?
16. What is the purpose of `show_sql=true` in Hibernate configuration?
17. What is required for a successful data save using Hibernate?
18. Why might an initial attempt to save data in Hibernate fail?
19. Which file is typically configured first in a Hibernate project setup?
20. What is the primary purpose of Hibernate in Java applications?

====================================================================================
07 - Spring Getting started
====================================================================================
Here are the questions extracted from your HTML:

1. What is the role of the Spring MVC module?
2. Which of the following statements is TRUE about Spring Boot?
3. Which Spring Boot feature automatically configures components based on project dependencies?
4. What is the default embedded server provided by Spring Boot?
5. Which tool is used to generate Spring Boot projects with minimal setup?
6. What is the main benefit of Dependency Injection (DI) in Spring?
7. Which feature of Spring is used to separate cross-cutting concerns like logging?
8. What is the key principle of Inversion of Control (IoC)?
9. What annotation is used in Spring to automatically inject dependencies?
10. Which Spring module is used for integrating ORM tools like Hibernate?
11. What does POJO stand for in the context of Spring?
12. What is the primary purpose of the Spring Framework?


====================================================================================
08 - Spring Framework – XML Bean Configuration Round at Telusko
====================================================================================
In an XML-based Spring configuration, which XML element is used to define a Spring bean - bean
Role Play
<component>
<bean>
<class>
<property>

Which Interface is used to load and manage Spring beans in the code? - ApplicationContext
Role Play
ApplicationContext
CourseSelecting
LaunchTestApp
ClassPathXmlApplicationContext

Which annotation injects a dependency into a Spring bean? - Autowired
Role Play
@Autowired
@Inject
@Resource
@Value

What does the ref attribute in <constructor-arg> specify in Spring? - Referenced Spring bean for injection
It specifies the class name
It specifies the constructor name
Referenced Spring bean for injection
It specifies the scope of the bean

Which autowiring mode uses property names to find matching beans for injection? - autowire="byName"
autowire="byName"
autowire="byType"
autowire="constructor"
autowire="no"

In XML configuration, which of the following attributes in the <bean> tag is used for setter injection? - <property>
<constructor-arg>
<set-property>
<property>
<value>

Which method is used to retrieve a bean from the Spring IOC container by its ID? - getBean()
getContext()
findBean()
getBean()
getObject()

How does Spring resolve conflicts when multiple beans of the same type exist? - Using the primary attribute on one bean
Using the primary attribute on one bean
By using the last defined bean
By choosing beans in alphabetical order
None of the above

The @ConstructorProperties annotation in Spring allows Spring to: - Map constructor parameters by their names
Inject dependencies based on bean IDs
Inject dependencies in any specified order
Map constructor parameters by their names
Bypass the autowiring process

Which attribute is used to specify that a bean should be created only when requested? - lazy-init
primary
lazy-init
id
autowire

Which method of getBean() allows you to retrieve a bean by type only if there is a single instance? - getBean(Class<T> type)
getBean(Class<T> type)
getBean(String id, Class<T> type)
getBean(String id)
getBean(id, type)

Which annotation is used to specify the scope of a Spring bean? - @Scope
@Scope
@Qualifier
@Primary
@Bean

What is the purpose of the class attribute in the <bean> element of Spring XML configuration? - It specifies the bean's class name
It defines the scope of the bean
It specifies the bean's class name
It sets the primary key of the bean
It injects properties into the bean 

What is the default scope for Spring beans if it is not specified explicitly? - Singleton
Singleton
Prototype
Request
Session
------
Your goals
Explain the difference between constructor and setter injection using Spring XML syntax

Understand how to wire beans using the ref attribute and define relationships between beans

Demonstrate awareness of bean scopes like singleton vs prototype

Discuss how Spring handles autowiring, including resolving conflicts using @Primary or matching by type

Describe lazy loading, use of inner beans, and wiring by interface for decoupling

====================================================================================
09 - Spring Java-Based Configuration Interview – at Telusko
====================================================================================
What is the purpose of the @Configuration annotation in Spring? -To mark a class as a source of bean definitions
To define the scope of beans
To mark a class as a source of bean definitions
To initialize Spring context
To specify bean dependencies

What is the default scope of a Spring bean? - Singleton
Prototype
Singleton
Request
Session

Which of the following annotations is used to define a bean in a configuration class? - @bean

What does AnnotationConfigApplicationContext do in Spring? - b
It initializes beans defined in XML files
It initializes beans using annotations in a configuration class
It provides a way to autowire beans
It initializes beans from property files

What is the default name of a Spring bean defined using @Bean? - c
The fully qualified class name
The class name in lowercase
The method name that defines the bean
No default name is assigned

Which annotation can be used to specify multiple names for a bean? - c
@Primary
@Qualifier
@Bean(name = {...})
@Autowired

How can you specify the default bean to inject when multiple candidates are present? -c
Using @Qualifier
Using @Autowired
Using @Primary
Using @Configuration


What is the output of the following code snippet if the bean has a prototype scope? - b
Desktop dt1 = context.getBean("desktop Prototype", Desktop.class);
Desktop dt2 = context.getBean("desktop Prototype", Desktop.class);
System.out.println(dt1 == dt2);
true
false
Compilation error
Run Time Error

Which of the following is true for constructor-based autowiring? - b
It is not recommended in newer versions of Spring
It ensures dependencies are injected during object creation
It is less preferred compared to field-level autowiring
It cannot handle multiple dependencies

In field-level autowiring, where is the @Autowired annotation placed? -c
On the setter method
On the constructor
Directly on the field
On the @Bean definition

What is the role of the @Qualifier annotation? - b
It marks a bean as a singleton
It allows specifying which bean to inject when multiple beans of the same type exist
It defines the lifecycle of a bean
It initializes beans at runtime

Which annotation is used to change the default bean scope to prototype? - c
@Prototype
@Bean(scope = "prototype")
@Scope("prototype")
@Autowired

What happens if there are multiple beans of the same type and no @Qualifier or @Primary is used? - a
Spring throws a runtime exception
Spring randomly picks a bean
The application runs without injecting any bean
Spring logs a warning and continues

Which autowiring method is most suitable for optional dependencies? - c
Field-level autowiring
Constructor-based autowiring
Setter-based autowiring
None of the above

What is the benefit of using constructor-based autowiring over field-level autowiring? - b
It reduces code complexity
It makes the class immutable and ensures dependencies are mandatory
It allows injecting multiple beans of the same type without ambiguity
It is the default behavior in Spring

----
Your goals
Differentiate between @Component vs @Bean and when to use each

Understand @Scope, @Value, and @Qualifier annotations in practical terms

Explain field, setter, and constructor-based autowiring

Configure beans using pure Java without XML

Perform live coding tasks based on realistic Telusko scenarios


====================================================================================
10 -Spring Boot Fundamentals + Architecture Round – at Telusko
====================================================================================
Which feature of Spring Boot allows applications to run without deploying to an external server? - b
Simplified configuration
Embedded servers
Annotations
Application Context

What does the @SpringBootApplication annotation do in a Spring Boot application?-b
Configures beans manually
Enables auto-configuration and component scanning
Adds all JAR dependencies automatically
Specifies the package for scanning

Which Spring Boot annotation defines a class as a Spring-managed component? -a
@Component
@Value
@Autowired
@Configuration

The @Repository annotation is primarily used in which layer? -c
Controller
Service
Data Access (DAO)
Security

Which MVC layer handles HTTP requests in Spring Boot? - c
Model
View
Controller
Service

In which layer does the application interact directly with the database in Spring Boot? - c
Controller
Service
Repository
View

What is the purpose of the @Repository annotation in Spring Boot? - b
Denotes a class as a controller
Marks a class as a bean and handles database exceptions
Creates a new bean for each HTTP request
Automatically starts the embedded server

Which annotation would you use to manage application properties in Spring Boot? - b
@Service
@Value
@Component
@Repository

A common annotation for defining a RESTful endpoint in Spring Boot is:  - b
@Service
@RestController
@Configuration
@Repository

Which Spring Boot method retrieves the LaptopService bean from the application context in 
SpringBoot DemoApplication? - c
context.getBean (Laptop.class)
SpringApplication.run()
context.getBean (LaptopService.class)
LaptopService.add()

----
Your goals
Explain the benefits of moving from Spring to Spring Boot

Use key annotations like @SpringBootApplication, @RestController, @Service, and @Repository

Understand and build layered architecture

Handle a basic REST API setup using Spring Boot

Solve 2–3 live coding tasks confidently
====================================================================================
11 - Spring JDBC Hands-On Interview – TeluskoD
====================================================================================
In Spring JDBC, which class is used to simplify database operations? - a
JdbcTemplate
JdbcConfig
DatabaseConnector
DataMapper

What does the update() method in JdbcTemplate return? -b
Number of rows retrieved
Number of rows affected
Total columns updated
A ResultSet object

What does the @Scope("prototype") do when applied to a Spring Bean? - a
Creates a new instance for each request
Shares a single instance globally
Makes it session-scoped
Disables the bean

What is the purpose of the schema.sql file in a Spring Boot project? - b
To define the initial data for the database
To define the database structure
To configure application properties
To set up custom SQL functions

What is the primary purpose of the data.sql file? - a
To insert initial data into tables
To update database records
To drop existing tables
To define the schema of the database

Where should the schema.sql and data.sql files be located in a Spring Boot project? - d
src/main/java
src/main/config
src/test/resources
src/main/resources

In the mapRow() method, what does the ResultSet parameter represent? - b
The current database connection
The SQL query being executed
The results from the SQL query
The number of affected rows

What does jdbc.update() do in the save() method? - b
Executes a SELECT statement
Inserts data into the database
Deletes data from the database
Maps a row to a Java object

In the application.properties file, which property specifies the JDBC URL for connecting to the
PostgreSQL database? - d
spring.datasource.username
spring.datasource.password
spring.datasource.driver-class-name
spring.datasource.url

Which method in JdbcTemplate is used to execute a SELECT query? - b
execute()
query()
find()
select()
----

Your goals
Explain what Spring JDBC is and how it differs from plain JDBC in terms of abstraction and productivity during interviews.

Set up a basic Spring JDBC application with proper configuration, demonstrating understanding of how to connect to a database.

Respond to short, practical coding prompts like writing a method to fetch a record using JdbcTemplate. (Example coding task in chat: “Can you write a method using JdbcTemplate to get student by ID?”)

Demonstrate how to use RowMapper properly to convert result sets into domain objects quickly. (Example coding task in chat: “Can you write a RowMapper for a Student class?”)

Answer real-world interview questions confidently, such as how exceptions are handled, or how to use schema/data.sql in Spring Boot JDBC.

====================================================================================
12 - Spring Boot Web Interview – Telusko Round with Akshay
====================================================================================
Which component of Spring MVC is responsible for representing the application's data? - b
View
Model
Controller
DispatcherServlet

What is the purpose of the @RequestMapping annotation in Spring MVC? - a
To map HTTP requests to specific methods
To define the model for the view
To bind form data to objects
To handle database operations

What is the main function of the View component in Spring MVC? - c
Handle user input processing
Manage application data representation
Present data to the end user
Oversee the application's control flow

What does the @ModelAttribute annotation do? - a
Links form data to model objects
Validate user input
Manages user requests
Defines the name of the view

What is the role of the DispatcherServlet's front controller role? - b
Displays data to the user
Manages HTTP requests centrally
Validates form data
Manages database connections

Which configuration file is required for setting up DispatcherServlet in a Spring MVC application? - b
application.properties
web.xml
dispatcher-servlet.xml
settings.xml

Which configuration file is required for setting up DispatcherServlet in a Spring MVC application?
application.properties
web.xml
dispatcher-servlet.xml
settings.xml

What does <servlet-mapping> do in the web.xml configuration? - a
Maps a servlet to a specific URL
Specifies the view resolver
Defines the servlet's package
Configures database connections

What does the Internal ResourceViewResolver bean do? -b 
Configures request handling
Resolves logical view names to files
Manages database connections
Handles HTTP sessions

If a controller returns a view name "home", which path does Spring MVC resolve if prefix is /views/and
suffix is.jsp?
/home.jsp
/views/home
/views/home.jsp
/home.html

Which of the following is a valid view technology supported by Spring Boot for web-based applications?- d
Thymeleaf
JSP
FreeMarker
All of the above

----
Your goals
Demonstrate understanding of controller and servlet mapping in Spring MVC, including how to configure @RequestMapping or @GetMapping for handling web requests.

Explain the flow of data from a web form to a Spring controller using @ModelAttribute and @RequestParam.

Coding Task: Write a short Spring controller method that maps a GET request to /welcome and returns a string "welcome-page" as the view name.

Coding Task: Given a form with name and email fields, write a Spring controller method that accepts the data using @ModelAttribute and prints it to the console.

Analyze a sample code snippet (shown in the interview) and explain what each annotation does and how the model data is passed to the view.
====================================================================================
13 - Exploring Web MVC without Spring Boot 
====================================================================================
In a Spring MVC app running on Tomcat, which component first receives every HTTP request and routes it to controllers? - c 
ViewResolver
HandlerInterceptor
DispatcherServlet
none of the above

Where do you map requests to the DispatcherServlet in a plain Maven Spring MVC project? - b 
application.properties
web.xml using <servlet> and <servlet-mapping>
pom.xml
server.xml

In web.xml, what must match to link the servlet definition with its mapping?
<url-pattern> and <display-name>
<servlet-class> and <url-pattern>
<
servlet-name> in both <servlet> and <servlet-mapping>
<load-on-startup> values

If your servlet name is telusko, what configuration file name does the DispatcherServlet look for by default? - b
applicationContext.xml in
/WEB-INF
telusko-servlet.xml
in
/WEB-INF
dispatcher-servlet.xml in project root
spring-mvc.xml in /resources

What two things must you typically declare in *-servlzet.xml so Spring can find annotation-based controllers? - b
<tx:annotation-driven> and <mvc:interceptors>
<ctx:component-scan> (base package) and <ctx: annotation-config>
<bean id="dataSource"> and <bean id="sessionFactory">
<bean id="dataSource"> and <bean id="sessionFactory">

In the debugging flow described, a 404 after hitting a controller usually indicated: - b
Tomcat is down
The view name (e.g., "index") couldn't be resolved to a JSP
The controller method threw a NullPointerException
The port is already in use

In a Spring MVC configuration, what does the mvc:resources tag do? - a
Configures static resource handling such as CSS, JavaScript, or images.
Scans the application for annotated controllers.
Maps URL patterns to controller methods.
Specifies the location of JSP files in the project.

What is the default scope of a Spring MVC controller? - a
Singleton
Prototype
Request
Session

Which bean resolves logical view names like "index" to actual JSP files? - c
UrlBasedViewResolver
ContentNegotiatingViewResolver
Internal Resource View Resolver
StaticViewResolver

What is the purpose of the @PathVariable annotation in Spring MVC?
To bind query parameters to a method argument.
To inject a request header into a method argument.
To bind URL template variables to method arguments.
To map a method directly to a URL.
----
Your goals
Explain the role of DispatcherServlet, request mapping, and the flow between controllers and views during an interview-style discussion.

Discuss and identify differences between XML-based and Java-based configuration for DispatcherServlet setup and view resolvers.

Write a simple @Controller class with a method that maps /hello to a view named welcome.jsp.

Implement a Java-based configuration that defines an InternalResourceViewResolver pointing to /WEB-INF/views/ and .jsp suffix.

Answer follow-up questions confidently, justify code decisions, and explain MVC flow using real-world terms.

====================================================================================
15 - RESR using Spring boot
====================================================================================
Which of the following is NOT a valid HTTP method used in REST APIs? - c 
GET
POST
UPDATE
DELETE

Which HTTP method is used to retrieve data from the server? - b
PUT
GET
DELETE
POST

What is the purpose of the HTTP PUT method? - b
To delete a resource
To update or replace a resource
To retrieve a resource
To create a new resource

In REST architecture, which of the following is a feature? - a
Stateless
Stateful
Server-dependency
Dynamic routing

What is the purpose of Postman in REST API development? - c
To build frontend applications
To debug backend code
To test and interact with REST APIs
To host a database

Which annotation is used to bind a method parameter to a URL template variable in Spring Boot? - b
@RequestBody
@PathVariable
@RequestParam
@RequestHeader

What is the purpose of the @RequestBody annotation in Spring Boot? - b
To map URL parameters
To pass JSON or XML data to a method
To define a request's HTTP header
To define a service endpoint

Which of the following best describes the difference between @PathVariable and @RequestParam?
@PathVariable maps to query parameters, @RequestParam maps to URL segments
@PathVariable maps to URL segments, @RequestParam maps to query parameters
Both map to query parameters
Both map to URL segments

Which annotation is used to handle HTTP PUT requests in Spring Boot? - a
@PutMapping
@PatchMapping
@RequestMapping
@PostMapping

Which HTTP library is commonly used in React to make API calls to a Spring Boot backend? - b
Express.js
Axios
Lodash
Mocha
------
Your goals
Explain REST principles and HTTP methods used in Spring Boot.

Describe how to configure a @RestController and map endpoints using @RequestMapping.

Code Practice 1: Create a simple @RestController with one GET endpoint returning a greeting message.

Code Practice 2: Add a POST method that accepts a JSON object and returns a confirmation string.

Confidently demonstrate the ability to test REST APIs using Postman and interpret results.

====================================================================================
16- spring Data JPA 
====================================================================================
Which pattern does Spring Data JPA follow? - b
Singleton Pattern
Repository Pattern
Factory Pattern
Observer Pattern

What does the findAll() method do in a repository? - c
Fetches a single record
Deletes records from the database
Retrieves all records associated with the entity
Updates an existing record

What type of data structure is used to hold the return value of findById()? - c
List
Array
Optional
Set

Which annotation is used to specify a custom query in Spring Data JPA? -a 
@Query
@FrameCustomQuery
@RepoQuery
@JPAQuery

Which repository interface allows you to execute stored procedures in Spring JPA? - c
CrudRepository
PagingAndSorting Repository
JpaRepository
Transactional Repository

How is pagination supported in Spring Data JPA? - b
By manually writing SQL for pagination
By using the Pageable interface
By default in all repository methods
By limiting the number of entities in the database

Which dependency is required to use Spring Data JPA with PostgreSQL? - c
spring-boot-starter-data-mysql
spring-boot-starter-data-jdbc
spring-boot-starter-data-jpa + postgresql driver dependency
spring-boot-starter-web

Which class is used to handle business logic in a Spring Data JPA application? - b
Repository
Service
Controller
Entity

What is the purpose of the @GetMapping annotation in the RestController? - b
To define a POST endpoint
To define a GET endpoint
To define a DELETE endpoint
To define a PUT endpoint

----
Your goals
Understand the basic purpose of Spring Data JPA and what problems it solves.

Be able to define a JPA Entity class and map it to a database table.

Use a JpaRepository to perform basic CRUD operations.

Understand how method naming conventions work for query generation.

Create a custom query using the @Query annotation in a repository interface.

====================================================================================
17.Spring Data Rest
====================================================================================
What does Spring Data REST automatically expose for Spring Data repositories? -c
SOAP endpoints
GraphQL endpoints
RESTful endpoints
FTP endpoints

Which request method is used to update an entity in Spring Data REST? - b
GET
PUT
POST
DELETE

How to retrieve HATEOAS links in the response from a Spring Data REST API? - b
Enable detailed logging
Perform a GET request to the resource
Include a specific header in the request
Adjust the database configuration

What action does "GET /users/{id}" perform in a RESTful API? - d
Deletes a user
Retrieves all user details
Creates a new user
Retrieves the details of a specific user

What does the @RequestBody annotation do in Spring? - b
Specifies the HTTP method
Deserializes the request body into an object
Defines the response body
Marks a class as a request body

In Spring Data REST, which HTTP method is used to create a new resource? - d
GET
DELETE
PUT
POST

Which annotation is used to indicate a Spring Data REST repository? - b
@RestController
@RepositoryRestResource
@SpringBootApplication
@Component

What is the role of HATEOAS links in the API response? - b
It stores user credentials
It provides navigation between related resources
It creates new database records
It manages application configurations

What does the repository layer in Spring Data REST manage? - c
User interface components
Database connections
CRUD operations for entities
Network communications

Which type of events can be captured in Spring Data REST using repository events? - c
Application logs
Metrics related to database performance
BeforeCreate and AfterDelete events
Information about user sessions

---
Your goals
Explain what Spring Data REST is and how it helps eliminate boilerplate controller code.

Describe how to expose a JPA repository as a RESTful endpoint using Spring Data REST annotations.

Define a simple Product entity with id and name, and create a repository interface that exposes it via Spring Data REST (without writing controller code).

====================================================================================
19. Spring AOP
====================================================================================
What is the main purpose of aspect-oriented programming (AOP)? - b
To increase code duplication
To handle cross-cutting concerns
To replace object-oriented programming
To enforce coding standards

Which of the following is an example of a cross-cutting concern? - b
Business logic
Logging
Database connection
Ul rendering

What annotation is used to mark a class as an aspect in Spring AOP? - a
@Aspect
@Component
@Service
@Controller

What is a Join Point in Spring AOP? - c
The method being executed
The action taken by an aspect
A point during the execution of a program
The object being advised

Which AOP advice runs before a matched method is executed? - b
@After
@Before
@AfterThrowing
@AfterReturning

What is a Pointcut in AOP? - c
The module encapsulating cross-cutting concerns
The action taken at a specific Join Point
The expression defining where advice should apply
The object being advised

What does @AfterThrowing advice do? - b
Executes before the method
Executes after a method throws an exception
Executes after the method successfully returns
Logs method execution time

Which of these annotations is used for performance monitoring in AOP? - d
@Before
@After
@AfterReturning
@Around

What does the args(postld) expression in a Pointcut signify? - b
The method name
The arguments passed to the method
The return type of the method
The target class of the method

What is an AOP Proxy in Spring? - b
A module that handles cross-cutting concerns
A dynamically created object that represents the target
A point during the execution of a program
An annotation for advice

---
Your goals
Explain the core concept of AOP and its use cases in Spring Boot projects.

List and describe the purpose of Before, After, and Around advice in AOP.

Write a basic @Aspect class with a @Before advice that logs any method in a UserService class.

Explain what a JoinPoint is and how it's used inside advice methods.

====================================================================================
20. Spring Security 
====================================================================================
What is the primary purpose of Spring Security? - c
Database management
Managing HTTP requests
Securing applications
Optimizing performance

What annotation is used to enable Spring Security in a Spring Boot application?- a
@EnableWebSecurity
@EnableSpringSecurity
@EnableSecurity
@EnableAuthentication

By default, Spring Security secures all endpoints of a Spring Boot application: - a
true
false

Which class is commonly extended to customize Spring Security configuration before spring security 6? - b
SecurityFilterChain
WebSecurityConfigurerAdapter
AuthenticationManager
SecurityContextHolder

Which component is responsible for authenticating user credentials in Spring Security? - b
Security FilterChain
Authentication Provider
UserDetailsService
AccessDecisionManager

What is the default encryption algorithm used by Spring Security for passwords in Spring Boot 5+? - c
MD5
SHA-1
BCrypt
PBKDF2

What is the purpose of @PreAuthorize in Spring Security? - b
To validate input parameters
To define method-level security
To authenticate users
To encode passwords

Which of these is used to configure role-based access control in Spring Security? - b
@EnableRoles
hasRole('ROLE_NAME')
EnableWebSecurity
PasswordEncoder

What is the purpose of SecurityContextHolder in Spring Security? - b
To manage password encryption
To store authentication details
To configure authorization rules
To create user sessions

Which interface in Spring Security defines the contract for user authentication? - a
Authentication
UserDetailsService
GrantedAuthority
AccessDecisionManager

In Spring Security, how is a user authenticated by default? - c
Using a predefined username and password
Using an external service
Using the UserDetails Service interface
Using a database connection

What does the csrf() method in the Spring Security configuration do? - a
Enables Cross-Site Request Forgery protection
Disables Cross-Site Scripting
Configures request validation
Manages cookie settings

Which filter is responsible for handling username and password-based authentication in Spring Security? - b
JwtAuthenticationFilter
Username PasswordAuthenticationFilter
BasicAuthenticationFilter
CsrfFilter

What is the primary purpose of the BCryptPasswordEncoder in Spring Security? - c
To decode passwords for verification.
To encrypt user data.
To hash passwords securely.
To store passwords in plain text.

Which annotation is used to enable method-level security with Spring Security? - a
@EnableMethodSecurity
@EnableWebSecurity
@EnableAuthorization
@EnableSecurity

---
Your goals
Explain the default login form behavior in Spring Security and how to customize it.

Demonstrate how CSRF protection works and what issues may arise without it.

Implement a short snippet to define a User entity and expose it through a repository.

Show how to hash passwords using BCrypt and configure a PasswordEncoder.

Describe the responsibilities of UserDetailsService and AuthenticationProvider.

====================================================================================
22. JWT(JSON WEB TOKEN) and OAuth2
====================================================================================
What algorithm is commonly used to sign a JWT? - d
AES
RSA
SHA-256
HMAC-SHA256

What type of information is typically stored in the payload of a JWT? - c
Encrypted data
Authentication keys
Claims like user roles or expiration time
Application settings

Which header parameter specifies the algorithm used to sign a JWT? - a
alg
typ
sig
enc

What does the 'exp' claim in a JWT indicate? - b
The time when the JWT was issued
The expiration time of the JWT
The user role
The algorithm used for signing

How can a JWT be verified on the server side? - b
By decoding the payload directly
By comparing the signature using a secret key
By matching it with a stored JWT in the database
By using a hashing function on the header

Which claim is used to specify the issuer of a JWT? - a
iss
sub
aud
exp

What is a key advantage of using JWT for authentication? - b
It does not require a server.
It is lightweight and stateless.
It is encrypted by default.
It eliminates the need for tokens

What is the primary purpose of OAuth? - b
Encrypting user data
Delegating access without sharing credentials
Securing databases
Encrypting network communications

What is the typical expiration time of a JWT access token? - c
24 hours
Unlimited
Configurable by the server
1 hour

What is the purpose of the "refresh token" in OAuth? - a
To extend the expiration time of an access token.
To store user session data.
To replace the need for an access token.
A refresh token for renewing sessions

In OAuth, who issues the access_token? - c
Resource Server
Client Application
Authorization Server
User Agent

What is the purpose of the scope parameter in OAuth? - b
To specify the token expiration time
To define the level of access requested
To verify the client's identity
To encrypt the token

What does a JSON Web Token (JWT) consist of? - b
Header and Payload
Header, Payload, and Signature
Payload and Signature
Header and Signature

Which part of a JWT is used to verify that the token has not been tampered with? - c
Header
Payload
Signature
Scope

---
Your goals
Explain how JWT works and why it’s used over session-based authentication.

Demonstrate the structure and purpose of JWT header, payload, and signature.

Implement a basic JWT generation logic using io.jsonwebtoken (JJWT or similar).

Describe how a OncePerRequestFilter is used to validate and extract JWT from headers.

Show understanding of OAuth2 login flow using Google or GitHub.

====================================================================================
23. Docker
====================================================================================
What is Docker primarily used for? - b
Network Virtualization
Containerization
Cloud Storage
Virtual Machine Management

Where can images of different software be found in the Docker ecosystem? - d
Docker Engine
Docker Volume
Docker Compose
Docker Hub

What is the purpose of Docker Volumes in the context of Docker? - c
To store Docker images
To manage the Docker Engine
To store data permanently
Containerization of applications

What does the "Pull" option do in Docker when searching for images? - b
Execute the image
Download the image
Run the container
Share the image with others

What does the command 'docker run image-name' do? - c
Display the image details
Download the image
Run the image as a container
Remove the image from the system

What information does the command 'docker ps' provide? - c
List of all containers
List of completed containers
List of currently running containers
Details of Docker images

What is the purpose of the command 'docker rm containerld'? - b
Remove the Docker image
Delete the container based on its ID
Run the container
Download the container

Which tool is used to create multiple containers for an application in Docker? - b
Docker Engine
Docker Compose
Docker Hub
Docker File

What issue does Docker address? - a
Inconsistencies in development environments
Lack of scalability in server deployment
Slow internet connection
Software licensing conflicts

Which analogy best describes the relationship between a Docker image and a Docker container? - b
Book and Library
Template and instance
None of the above
Object and Blueprint

---
Your goals
Explain the problem Docker solves compared to traditional virtualization.

Describe how to run a simple Spring Boot app inside a Docker container.

Demonstrate basic Docker commands like docker build, docker run, and docker ps.

Understand and briefly explain the structure of a Dockerfile.

Use Docker Compose to define and run a multi-container setup (Spring Boot + Postgres).

====================================================================================
24. Cloud Deployment
====================================================================================
What is the primary benefit of deploying a Spring Boot application to the cloud? - c
Reduced development time
Automatic code generation
Scalability and high availability
Faster compilation speed

Which AWS service is specifically designed for easy deployment of web applications without managing
underlying infrastructure? - b
Amazon EC2
AWS Elastic Beanstalk
Amazon S3
AWS Lambda

When deploying a Spring Boot application with a database to AWS, which service combination is most appropriate? - c
EC2 + DynamoDB
Lambda + Aurora
Elastic Beanstalk + RDS
ECS + S3

What does AWS RDS primarily provide? - b
Container orchestration
Managed relational database service
Static file hosting
Load balancing

Which AWS service is used to store and manage Docker container images? - b
AWS ECS
AWS ECR
AWS EKS
AWS Fargate

In AWS ECS, what is a Task Definition? - b
A running instance of a container
A blueprint that describes how containers should run
A cluster of EC2 instances
A load balancer configuration

What is the purpose of AWS CLI in Spring Boot cloud deployment? - c
To write Spring Boot code
To compile Java applications
To interact with AWS services from command line
To debug applications

When pushing a Docker image to AWS ECR, which authentication method is typically required? - b
Username and password
AWS CLI configured with proper IAM permissions
SSH keys
API Gateway tokens

What file is typically used to define environment-specific configurations in Spring Boot for cloud deployment? - b
pom.xml
application.properties or application.yml 
web.xml
Dockerfile

In AWS ECS, what is the difference between EC2 Launch Type and Fargate Launch Type? - b
EC2 is for containers, Fargate is for virtual machines
EC2 requires server management, Fargate is serverless
EC2 is cheaper, Fargate is more expensive
EC2 supports Java, Fargate supports only Python

---
Your goals
Explain the purpose of Elastic Beanstalk and ECS in simple terms.

Describe the steps to deploy a Spring Boot app using AWS Elastic Beanstalk.

Use AWS CLI to configure credentials and deploy a Docker image.

Understand how ECR works for storing Docker images.

Successfully demonstrate basic cloud deployment steps using simple CLI or YAML commands.

====================================================================================

====================================================================================



====================================================================================
25. Microservices
====================================================================================
Which dependency is required for implementing API Gateway in a Spring Boot microservices architecture? - b
Spring Boot Starter Security
Spring Cloud Gateway
Spring Boot Starter Actuator
Spring Boot DevTools

Which of the following is a Spring Boot tool for simplifying HTTP-based inter-service communication? - a
Feign Client
RabbitMQ
Redis
JDBC

How does Spring Cloud Config Server help in microservices? - b
Caches microservice data
Manages and externalizes configuration for distributed services
Handles user authentication
Provides service discovery

What is the role of Hystrix in Spring Boot microservices? - c
Service discovery
API Gateway routing
Circuit breaker for fault tolerance
Monitoring service health

Which of these is NOT a recommended communication method for Spring Boot microservices? - c
REST APIs
Feign Clients
JDBC direct queries
Message queues like RabbitMQ or Kafka

What is the primary benefit of using circuit breakers in microservices? - b
Speeding up database queries
Preventing cascading failures when a service is down
Improving Ul responsiveness
Reducing the number of services needed

Which tool in Spring Boot helps monitor the health of microservices? - b
Spring Boot DevTools
Spring Boot Actuator
Spring Boot CLI
Spring Boot Validator

What does Spring Boot's embedded Tomcat server provide in a microservices context? - b
Scalability
Independent deployment capability
Centralized service registry
Database management

In a Spring Boot microservices project, what is used to run multiple instances of a service? - b
Adding more controllers
Running the same service on different ports
Configuring a single instance in the application.properties file
Using shared session storage

What is the purpose of using @EnableEurekaServer in a Spring Boot application? - b
To enable load balancing
To set up a Eureka server for service registration and discovery
To configure an embedded database
To secure REST endpoints

---
Your goals
Understand the core concept of microservices and how they differ from monolithic architecture.

Show a simple Spring Boot annotation-based config to register a service with Eureka.

Use Spring Cloud components like Eureka for service discovery and Feign for inter-service communication.

Implement basic load balancing and understand the role of Spring Cloud Gateway in routing.

Be able to create and run multiple Spring Boot services communicating with each other.

====================================================================================
27.git
====================================================================================
What type of Version Control System is Git? - c
Local Version Control System
Centralized Version Control System (CVCS)
Distributed Version Control System (DVCS)
Bitbucket

Which Git command creates an empty local repository in a directory? - b
git start
git init
git create
git new

What is the primary purpose of the 'git clone' command in Git? - c
To create a new branch
To merge branches
To copy a remote repository to your local machine
To switch between branches

What is the purpose of the 'git status' command? - c
To initialize a new Git repository
To view the history of commits
To see the current status of your working directory and staged changes
To create a new branch

What do you mean by an "untracked file" in Git? - c
A file that has been committed to the repository
A file that is currently staged
A file that is in the working directory but not yet tracked by Git and has not been committed
A file that Git ignores

How do you perform a commit in Git? - c
git commit -a
git commit -message "Commit message"
git commit -m "Commit message"
git save

How do you connect your local Git repository to a remote repository? - b
git link <remote-url>
git remote add origin <remote-url>
git connect <remote-url>
git attach <remote-url>

What happens if you try to switch branches in Git without committing the changes in your current branch? - c
Git automatically commits the changes
Git asks you to discard the changes
Git primarily warns the user and suggests appropriate actions (stashing or committing)
Git creates a new branch with the changes

In Git, what does "origin" refer to? - d
The latest commit in the repository
The current branch
The local Git configuration file
The default name for a remote repository

---
Your goals
Understand the core purpose of Git and how it enables version control and collaboration.

Know how to initialize a repository, make commits, and track file changes using Git.

Demonstrate the use of git branch, merge, and rebase, and explain when to use which.

Simulate pushing to and pulling from a remote repository (e.g., GitHub).

Resolve a simple merge conflict or explain the process with confidence
====================================================================================
28. DSA (Optiona)
====================================================================================
What defines a full binary tree?
What traversal visits the left subtree, then root, then right subtree in a binary tree?
What is the time complexity to insert a node at the beginning of a singly linked list?
Which data structure is best for implementing undo functionality?
In a circular queue, what happens when rear reaches the end but space is available at the front?
What is the correct time complexity to search for a value in a Binary Search Tree (average case)?
What happens when you pop from an empty stack?
Which operation adds an element to the top of the stack?
What does a stack follow?
In recursion, what does the base case represent?
Which sorting algorithm follows the divide-and-conquer approach?
What is the worst-case time complexity of Insertion Sort?
Which sorting algorithm finds the smallest element and places it at the beginning in each iteration?
Which sorting algorithm repeatedly compares adjacent elements and swaps them if needed?
What is the time complexity of accessing an element in an array by index?


